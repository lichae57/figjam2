<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - İnteraktif Şube</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/figjam2.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
</head>
<body>
    @if ((ViewData["ShowNavbar"] as bool?) ?? true)
    {
        <header class="bank-header">
            <nav class="navbar navbar-light bg-white shadow-sm">
                <div class="container d-flex justify-content-between align-items-center">
                    <a class="navbar-brand d-flex align-items-center" asp-area="" asp-page="@(User.Identity?.IsAuthenticated == true ? "/Dashboard" : "/Index")" style="margin-right: 0; padding-left: 0; background: white; padding: 0.5rem 1rem; border-radius: 8px; text-decoration: none;">
                        <img src="~/img/logo.png" alt="Interaktif Kredi" style="height: 55px; width: auto;" />
                    </a>
                    
                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        <!-- Giriş Yapmış Kullanıcılar İçin Header -->
                        <div class="d-flex align-items-center gap-3">
                            <a href="/CreditReport" class="btn" style="background-color: #E6F7FF; color: #2E6DF8; border: 1px solid #2E6DF8; border-radius: 12px; padding: 0.5rem 1.5rem; font-weight: 600; text-decoration: none; transition: all 0.3s ease;">
                                Kredi Raporları
                            </a>
                            <a href="/CustomerService" class="btn" style="background-color: #E6F7FF; color: #2E6DF8; border: 1px solid #2E6DF8; border-radius: 12px; padding: 0.5rem 1.5rem; font-weight: 600; text-decoration: none; transition: all 0.3s ease;">
                                Yardım ve Destek Merkezi
                            </a>
                            <!-- Profil Dropdown -->
                            <div class="dropdown">
                                <button class="btn dropdown-toggle" 
                                        type="button" 
                                        id="profileDropdown" 
                                        data-bs-toggle="dropdown" 
                                        aria-expanded="false"
                                        style="background-color: #E6F7FF; color: #2E6DF8; border: 1px solid #2E6DF8; border-radius: 12px; padding: 0.5rem 1.5rem; font-weight: 600;">
                                    Profil
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="profileDropdown" style="margin-top: 0.5rem; border: 1px solid #E6F7FF; box-shadow: 0 8px 24px rgba(46, 109, 248, 0.15); border-radius: 12px; padding: 1rem 0; min-width: 280px;">
                                    <!-- Kullanıcı Özeti -->
                                    <li>
                                        <div class="px-3 py-3 border-bottom" style="border-color: #E6F7FF !important;">
                                            <div class="d-flex align-items-center gap-3">
                                                <div style="width: 50px; height: 50px; border-radius: 50%; background: linear-gradient(135deg, #2E6DF8 0%, #0056B3 100%); display: flex; align-items: center; justify-content: center; overflow: hidden; border: 2px solid #E6F7FF; flex-shrink: 0;">
                                                    <img src="~/img/profile-placeholder.png" alt="Profil" style="width: 100%; height: 100%; object-fit: cover;" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                                                    <i class="fas fa-user" style="display: none; color: white; font-size: 1.2rem;"></i>
                                                </div>
                                                <div style="flex: 1; min-width: 0;">
                                                    <div class="fw-bold" style="color: #2E6DF8; font-size: 0.95rem; margin-bottom: 0.25rem;">@(ViewContext.HttpContext.Session.GetString("UserName") ?? User.Identity?.Name ?? "Ahmet Yılmaz")</div>
                                                    <div class="text-muted" style="font-size: 0.8rem; word-break: break-word;">@(ViewContext.HttpContext.Session.GetString("Identifier") ?? User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value ?? "ahmet.yilmaz@example.com")</div>
                                                </div>
                                            </div>
                                        </div>
                                    </li>
                                    <li><hr class="dropdown-divider my-2" style="margin: 0.5rem 0; border-color: #E6F7FF;"></li>
                                    <li>
                                        <a class="dropdown-item py-2 px-3" href="/Profile" style="transition: all 0.2s ease; display: flex; align-items: center; gap: 0.5rem; color: #333;">
                                            <i class="fas fa-user"></i>
                                            <span>Profil Detayları</span>
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item py-2 px-3" href="/Settings" style="transition: all 0.2s ease; display: flex; align-items: center; gap: 0.5rem; color: #333;">
                                            <i class="fas fa-cog"></i>
                                            <span>Ayarlar</span>
                                        </a>
                                    </li>
                                    <li><hr class="dropdown-divider my-2" style="margin: 0.5rem 0; border-color: #E6F7FF;"></li>
                                    <!-- Güvenli Çıkış -->
                                    <li>
                                        <a class="dropdown-item py-2 px-3 text-danger fw-semibold" href="#" onclick="logout(); return false;" style="transition: all 0.2s ease; display: flex; align-items: center; gap: 0.5rem;">
                                            <i class="fas fa-sign-out-alt"></i>
                                            <span>Oturumu Kapat</span>
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="d-flex align-items-center gap-3">
                            <div class="d-flex gap-2">
                                <a href="/Login" class="btn" style="background-color: #E6F7FF; color: #2E6DF8; border: 1px solid #2E6DF8; border-radius: 12px; padding: 0.5rem 1.5rem; font-weight: 600; text-decoration: none; transition: all 0.3s ease;">
                                    Giriş Yap
                                </a>
                            </div>
                        </div>
                    }
                </div>
            </nav>
        </header>
    }

    <main role="main" class="@((ViewData["ShowNavbar"] as bool?) ?? true ? "" : "full-height")">
        @RenderBody()
    </main>

    @{
        var currentPagePath = ViewContext.RouteData.Values["page"]?.ToString() ?? "";
        var requestPath = ViewContext.HttpContext.Request.Path.Value?.ToLower() ?? "";
        var isIndexPage = currentPagePath == "/Index" || currentPagePath == "/" || currentPagePath == "" || requestPath == "/" || requestPath == "/index";
        var showFooter = isIndexPage && ((ViewData["ShowFooter"] as bool?) ?? true);
    }
    
    @if (showFooter)
    {
        <footer class="bank-footer mt-5" style="background-color: var(--primary-color); color: var(--text-light);">
            <div class="container py-4 py-md-5">
                <div class="row g-4 justify-content-center">
                    <!-- Hakkımızda -->
                    <div class="col-12 col-lg-3 col-md-6 text-center text-md-start">
                        <h5 class="mb-4 fw-bold">
                            <i class="fas fa-university me-2"></i>
                            İnteraktif Şube
                        </h5>
                        <p class="mb-3" style="opacity: 0.9; line-height: 1.8;">
                            Dijital bankacılık deneyiminiz için yanınızdayız. Modern teknoloji ile güvenli ve hızlı hizmet.
                        </p>
                        <div class="social-icons">
                            <a href="#" class="text-white me-3" style="font-size: 1.5rem; opacity: 0.8; transition: opacity 0.3s;">
                                <i class="fab fa-facebook"></i>
                            </a>
                            <a href="#" class="text-white me-3" style="font-size: 1.5rem; opacity: 0.8; transition: opacity 0.3s;">
                                <i class="fab fa-twitter"></i>
                            </a>
                            <a href="#" class="text-white me-3" style="font-size: 1.5rem; opacity: 0.8; transition: opacity 0.3s;">
                                <i class="fab fa-instagram"></i>
                            </a>
                            <a href="#" class="text-white me-3" style="font-size: 1.5rem; opacity: 0.8; transition: opacity 0.3s;">
                                <i class="fab fa-linkedin"></i>
                            </a>
                            <a href="#" class="text-white" style="font-size: 1.5rem; opacity: 0.8; transition: opacity 0.3s;">
                                <i class="fab fa-youtube"></i>
                            </a>
                        </div>
                    </div>

                    <!-- Hızlı Linkler -->
                    <div class="col-12 col-lg-2 col-md-6 text-center text-md-start mx-auto mx-lg-0">
                        <h6 class="mb-4 fw-bold">Hızlı Linkler</h6>
                        <ul class="list-unstyled">
                            <li class="mb-2">
                                <a href="/Dashboard" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Ana Sayfa
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="/Services" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Hizmetler
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="/CreditCalculator" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Kredi Hesaplama
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="/Profile" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Profil
                                </a>
                            </li>
                        </ul>
                    </div>

                    <!-- Kurumsal -->
                    <div class="col-12 col-lg-2 col-md-6 text-center text-md-start mx-auto mx-lg-0">
                        <h6 class="mb-4 fw-bold">Kurumsal</h6>
                        <ul class="list-unstyled">
                            <li class="mb-2">
                                <a href="#" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Hakkımızda
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Yatırımcı İlişkileri
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Kariyer
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>İletişim
                                </a>
                            </li>
                        </ul>
                    </div>

                    <!-- Güvenlik & Destek -->
                    <div class="col-12 col-lg-2 col-md-6 text-center text-md-start mx-auto mx-lg-0">
                        <h6 class="mb-4 fw-bold">Güvenlik & Destek</h6>
                        <ul class="list-unstyled">
                            <li class="mb-2">
                                <a href="#" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Güvenlik
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Gizlilik Politikası
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>Kullanım Koşulları
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="text-white text-decoration-none" style="opacity: 0.8;">
                                    <i class="fas fa-chevron-right me-2" style="font-size: 0.7rem;"></i>KVKK
                                </a>
                            </li>
                        </ul>
                    </div>

                    <!-- İletişim -->
                    <div class="col-12 col-lg-3 col-md-6 text-center text-md-start">
                        <h6 class="mb-4 fw-bold">İletişim</h6>
                        <ul class="list-unstyled">
                            <li class="mb-3">
                                <div class="d-flex align-items-start">
                                    <i class="fas fa-phone-alt me-3 mt-1" style="opacity: 0.8;"></i>
                                    <div>
                                        <small class="d-block" style="opacity: 0.9;">Müşteri Hizmetleri</small>
                                        <strong>0850 222 0 222</strong>
                                    </div>
                                </div>
                            </li>
                            <li class="mb-3">
                                <div class="d-flex align-items-start">
                                    <i class="fas fa-envelope me-3 mt-1" style="opacity: 0.8;"></i>
                                    <div>
                                        <small class="d-block" style="opacity: 0.9;">E-posta</small>
                                        <strong>destek@@interaktifsube.com</strong>
                                    </div>
                                </div>
                            </li>
                            <li class="mb-3">
                                <div class="d-flex align-items-start">
                                    <i class="fas fa-map-marker-alt me-3 mt-1" style="opacity: 0.8;"></i>
                                    <div>
                                        <small class="d-block" style="opacity: 0.9;">Adres</small>
                                        <strong>İstanbul, Türkiye</strong>
                                    </div>
                                </div>
                            </li>
                        </ul>
                    </div>
                </div>

                <!-- Alt Bilgi -->
                <hr class="my-4" style="border-color: rgba(255,255,255,0.2);">
                <div class="row align-items-center">
                    <div class="col-12 col-md-6 text-center text-md-start mb-2 mb-md-0">
                        <p class="mb-0" style="opacity: 0.8;">
                            <i class="fas fa-shield-alt me-2"></i>
                            Güvenli ve Şifrelenmiş Bağlantı
                        </p>
                    </div>
                    <div class="col-12 col-md-6 text-center text-md-end">
                        <p class="mb-0" style="opacity: 0.8;">
                            &copy; 2025 İnteraktif Şube. Tüm hakları saklıdır.
                        </p>
                    </div>
                </div>
            </div>
        </footer>
    }

    <!-- Toast Container -->
    <div class="toast-container" id="toastContainer"></div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <script>
        // Dropdown hover functionality
        document.addEventListener('DOMContentLoaded', function() {
            const dropdowns = document.querySelectorAll('.navbar-nav .dropdown');
            
            dropdowns.forEach(dropdown => {
                const dropdownToggle = dropdown.querySelector('.dropdown-toggle');
                const dropdownMenu = dropdown.querySelector('.dropdown-menu');
                
                // Hover aç
                dropdown.addEventListener('mouseenter', function() {
                    const bsDropdown = new bootstrap.Dropdown(dropdownToggle);
                    bsDropdown.show();
                });
                
                // Hover kapat (menü dışına çıkınca)
                dropdown.addEventListener('mouseleave', function() {
                    const bsDropdown = bootstrap.Dropdown.getInstance(dropdownToggle);
                    if (bsDropdown) {
                        bsDropdown.hide();
                    }
                });
            });
        });

        // Sayfa yüklendiğinde overlay temizleme
        (function() {
            function cleanupOverlay() {
                // Tüm backdrop ve overlay'leri kaldır
                const backdrops = document.querySelectorAll('.modal-backdrop, .offcanvas-backdrop, [class*="backdrop"], [class*="overlay"]');
                backdrops.forEach(backdrop => {
                    backdrop.remove();
                    backdrop.style.display = 'none';
                    backdrop.style.visibility = 'hidden';
                    backdrop.style.pointerEvents = 'none';
                });
                
                // Body'den modal-open class'ını kaldır
                document.body.classList.remove('modal-open');
                
                // Body'nin overflow ve padding stillerini sıfırla
                document.body.style.overflow = '';
                document.body.style.paddingRight = '';
                document.body.style.position = '';
                document.body.style.pointerEvents = '';
                
                // Tüm fixed position overlay'leri bul ve kaldır
                const allElements = document.querySelectorAll('*');
                allElements.forEach(el => {
                    const computedStyle = window.getComputedStyle(el);
                    if (computedStyle.position === 'fixed' && 
                        (computedStyle.backgroundColor === 'rgba(0, 0, 0, 0.5)' || 
                         computedStyle.backgroundColor === 'rgb(0, 0, 0)' ||
                         el.classList.contains('backdrop') ||
                         el.classList.contains('overlay'))) {
                        el.style.display = 'none';
                        el.style.pointerEvents = 'none';
                        el.style.visibility = 'hidden';
                    }
                });
            }
            
            // Sayfa yüklendiğinde
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', cleanupOverlay);
            } else {
                cleanupOverlay();
            }
            
            // Window load olduğunda da temizle
            window.addEventListener('load', cleanupOverlay);
            
            // Her 1 saniyede bir kontrol et
            setInterval(cleanupOverlay, 1000);
        })();

        // Logout fonksiyonu
        function logout() {
            // Direkt olarak Logout sayfasına git (GET request)
            // Logout sayfası session'ı temizleyip ana sayfaya yönlendirecek
            window.location.href = '/Logout';
        }
    </script>

    <style>
        /* Header Stilleri */
        .bank-header {
            position: sticky;
            top: 0;
            z-index: 1000;
            background: white;
        }

        .bank-header .navbar {
            padding: 0.75rem 0;
            min-height: 70px;
        }

        /* Arama Çubuğu Stilleri */
        .search-container input::placeholder {
            color: #6c757d !important;
        }
        
        .search-container input:focus {
            background-color: #ffffff !important;
            border-color: #2E6DF8 !important;
            outline: none;
            box-shadow: 0 0 0 3px rgba(46, 109, 248, 0.1) !important;
        }
        
        .search-container input:focus::placeholder {
            color: #6c757d !important;
        }

        .search-container input:hover {
            border-color: #2E6DF8 !important;
        }

        /* Bildirim İkonu Stilleri */
        .notification-icon-container:hover i {
            color: #0056B3 !important;
            transform: scale(1.1);
        }

        .notification-badge {
            animation: pulse 2s infinite;
        }

        @@keyframes pulse {
            0%, 100% {
                opacity: 1;
            }
            50% {
                opacity: 0.7;
            }
        }

        /* Ayarlar İkonu Stilleri */
        .btn-link:hover i.fa-cog {
            transform: rotate(90deg);
            color: #0056B3 !important;
        }

        .btn-link i.fa-cog {
            transition: transform 0.3s ease, color 0.3s ease;
        }

        /* Header Navigation Linkleri */
        .nav-link-custom:hover {
            background-color: #E6F7FF !important;
            color: #0056B3 !important;
        }

        .nav-link-custom i {
            font-size: 0.9rem;
        }

        /* Profil Dropdown Stilleri */
        .dropdown-toggle::after {
            margin-left: 0.5rem;
            color: #2E6DF8;
        }

        .dropdown-menu {
            border: 1px solid #E6F7FF !important;
        }

        .dropdown-item:hover {
            background-color: #E6F7FF !important;
            color: #2E6DF8 !important;
        }

        .dropdown-item.text-danger:hover {
            background-color: #FFF5F5 !important;
            color: #DC3545 !important;
        }

        /* Giriş Yapmamış Kullanıcılar İçin Butonlar */
        .btn-outline-primary-custom {
            color: var(--primary-color);
            border-color: var(--primary-color);
            background-color: transparent;
            transition: all 0.3s ease;
        }

        .btn-outline-primary-custom:hover {
            color: var(--text-light);
            background-color: var(--primary-color);
            box-shadow: 0 4px 8px rgba(34, 40, 84, 0.2);
            transform: translateY(-2px);
        }

        /* Dashboard Header Butonları */
        .dropdown-toggle.btn:hover {
            background-color: #2E6DF8 !important;
            color: white !important;
            border-color: #2E6DF8 !important;
        }

        .btn[href="/CreditReport"]:hover,
        .btn[href="/CustomerService"]:hover {
            background-color: #2E6DF8 !important;
            color: white !important;
            border-color: #2E6DF8 !important;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(46, 109, 248, 0.3);
        }

        /* Responsive */
        @@media (max-width: 992px) {
            .nav-link-custom span {
                display: none;
            }

            .nav-link-custom {
                padding: 0.5rem !important;
            }
        }

        @@media (max-width: 768px) {
            .search-container input {
                width: 150px !important;
            }

            .notification-icon-container,
            .btn-link {
                padding: 0.25rem !important;
            }

            .dropdown-toggle span {
                display: none !important;
            }

            .nav-link-custom {
                display: none !important;
            }
        }
    </style>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>